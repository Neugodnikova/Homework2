#задаем переменную, в которую записываем слово
word = str(input('введите слово из латинских букв: '))
n = int(len(word)) % 2
m = int(len(word)) // 2
if int(len(word)) % 2==0:
     print('в слове четное число букв, вывод 2-х средних: ',str(word[m-1]),str(word[m]) )
else:
     print('в слове нечетное число букв, вывод 1-ой средней: ',str(word[m]) )
